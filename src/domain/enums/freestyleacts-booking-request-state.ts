export enum FreestyleActsBookingRequestState {
  REQUEST_PENDING = 'request_pending',
  OFFER_PENDING = 'offer_pending',
  ACCEPTED = 'accepted',
  REJECTED_BY_ARTIST = 'rejected_by_artist',
  REJECTED_BY_CLIENT = 'rejected_by_client',
}
