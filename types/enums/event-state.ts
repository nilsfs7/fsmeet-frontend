export enum EventState {
  CREATED = 'created',
  WAITING_FOR_APPROVAL = 'waiting_for_approval',
  APPROVED = 'approved',
  DENIED = 'denied',
  ARCHIVED_HIDDEN = 'archived_hidden',
  ARCHIVED_PUBLIC = 'archived_public',
}
